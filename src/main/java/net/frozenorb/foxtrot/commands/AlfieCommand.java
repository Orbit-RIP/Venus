package net.frozenorb.foxtrot.commands;

import net.frozenorb.foxtrot.Foxtrot;
import net.frozenorb.foxtrot.util.CC;
import cc.fyre.proton.command.Command;
import cc.fyre.proton.command.param.Parameter;
import org.bukkit.Bukkit;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

import java.util.UUID;

public class AlfieCommand {

//    @Command(names = {"alfiesfunnycommand"}, permission = "op")
//    public static void spamkill(Player sender, @Parameter(name = "player") Player player) {
//        for (int i = 0; i < 100; i++) {
//            player.setHealth(0);
//        }
//    }

//    @Command(names = {"togglenametagsabove", "togglenametagsabovehead", "togglenametagabove"}, permission = "foxtrot.togglenamtags", description = "command used to toggle lunar nametags incase of lag issues")
//    public static void togglelunarnametags(CommandSender sender, @Parameter(name = "yes|no") String yesOrno) {
//        if (Foxtrot.getInstance().getScoreboardManager().isLunarEnabled()) {
//            Foxtrot.getInstance().getScoreboardManager().setLunarEnabled(false);
//            sender.sendMessage(CC.translate("&fLunar nametags have been toggled off"));
//        } else {
//            Foxtrot.getInstance().getScoreboardManager().setLunarEnabled(true);
//            sender.sendMessage(CC.translate("&fLunar nametags have been toggled back on"));
////            if (yesOrno.equalsIgnoreCase("yes")) {
////                Bukkit.broadcastMessage(CC.chat("&aNameTags above head are now toggled back on for everyone on LunarClient or CheatBreaker!"));
//            }
//        }
    }


