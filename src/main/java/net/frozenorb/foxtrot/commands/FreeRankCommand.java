package net.frozenorb.foxtrot.commands;

import net.frozenorb.foxtrot.Foxtrot;
import net.frozenorb.foxtrot.util.CC;
import cc.fyre.proton.command.Command;
import org.bukkit.Bukkit;
import org.bukkit.entity.Player;

public class FreeRankCommand {

//    @Command(names = "claimrank", permission = "")
//    public static void freeRank(Player player) {
//        User profile = User.getByUUID(player.getUniqueId());
//        Rank rank = profile.getRank();
//        Rank warrior = Rank.getByName("Knight");
//
//        if (rank.getWeight() >= warrior.getWeight()) {
//            player.sendMessage(CC.translate("&cYou already have an equal or higher rank."));
//            return;
//        }
//
//        Bukkit.dispatchCommand(Bukkit.getConsoleSender(), String.format(
//                "setrank %s %s perm",
//                profile.getName(),
//                warrior.getName()
//        ));
//
//        Foxtrot.getInstance().getServer().broadcastMessage("");
//        Foxtrot.getInstance().getServer().broadcastMessage(CC.translate("&e" + player.getName() + " &fhas claimed their free &7Knight &fRank"));
//        Foxtrot.getInstance().getServer().broadcastMessage(CC.translate("&7&oYou can do the same by typing &e/claimrank"));
//        Foxtrot.getInstance().getServer().broadcastMessage("");
//
//
//    }
}

